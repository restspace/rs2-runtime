# 2.3 Writing a minimal tenant config

A **tenant config** declares one application: its mounts and a few top-level
settings. With `tenantsDir: "./tenants"` and tenant `main`, the file is
`./tenants/main.json`.

## The smallest useful tenant

```json
{
  "mounts": [
    { "path": "/files", "service": "file" }
  ]
}
```

That's a complete, valid tenant: a streamed file store at `/files`. `mounts` is
the only required key. Start the node (or reload — see below) and `/files` is
live.

## A more realistic starting point

```json
{
  "mounts": [
    { "path": "/files", "service": "file" },
    { "path": "/data",  "service": "data" },
    { "path": "/services", "service": "services",
      "config": { "access": { "read": "all", "write": "A" } } }
  ]
}
```

This gives you file storage, a JSON record store, and the **self-config API** at
`/services` — the last of which lets you edit the tenant over HTTP instead of
editing the file and restarting (Part 10). Most tenants want `services` mounted
from the start.

## Anatomy of a mount

```json
{ "path": "/data", "service": "data", "config": { "enforceSchema": true } }
```

- `path` — the URL prefix. Routing is longest-prefix on segment boundaries, so
  more specific mounts win.
- `service` — `file`, `data`, `pipeline`, `query`, `auth`, `services`, `log`, or
  a custom `code:<name>@<version>`.
- `config` — service-specific options plus the standard cross-cutting keys that
  work on **any** mount: `access`, `retry`, `caching`, and agent-surface
  metadata (`x-agent`, `description`, …). These are introduced where they're
  relevant and gathered in Appendix B.

Duplicate `path`s are rejected, and the whole config is validated before it goes
live — an invalid tenant never partially loads.

## Loading changes

- **Editing the file on disk** takes effect on a node restart (or any
  `PUT /services/raw` round-trip that triggers a reload).
- **Editing through `PUT /services/raw`** (the `services` mount) validates,
  dry-builds, and hot-swaps the tenant atomically with no restart — the
  preferred path on a running server.

For now, edit `./tenants/main.json` and restart the node. With a tenant in
place, let's make a real request.

---

← [Previous: 2.2 Running your first node](2.2-running-your-first-node.md) · [Manual home](../README.md) · [Next: 2.4 Making your first request →](2.4-your-first-request.md)

/target
/data
/logs
/data-store
conformance/echo-guest/target
conformance/echo-guest/Cargo.lock
bash.exe.stackdump
/.cargo/config.toml
# Runtime state created by running an example node (file/data stores).
examples/*/data/
examples/*/data-store/
# Local persistent dev nodes started by scripts/node-*.ps1
/.run/
# Internal strategy/positioning notes — not for the public repo.
/docs/positioning.md
# Dev-node log files.
/rs2-dev.out.log
/rs2-dev.err.log
# Prebuilt server binaries (release artifacts, not source).
/rs2-server-ubuntu18
# Local npm helper (personal SSH shortcut with private key path).
/package.json
# Stray agent worktrees.
/.claude/
/PUBLIC-RELEASE-HANDOFF.md
